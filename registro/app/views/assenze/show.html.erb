<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mostra Assenza</title>
    <link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="assets/css/styles.css">
</head>

<body style="background:#FFA726;">
    <div class="col-md-12", align="center">
        <p id="notice"><%= notice %></p>


<p>
  <strong>Data:</strong>
  <%= @assenza.date %>
</p>

<p>
  <strong>Cognome:</strong>
  <%= Alunno.find(@assenza.alunno_id).cognome %>
</p>

<p>
  <strong>Nome:</strong>
  <%= Alunno.find(@assenza.alunno_id).nome %>
</p>

<p>
  <strong>Cf:</strong>
  <%= Alunno.find(@assenza.alunno_id).cf %>
</p>

<p>
  <strong>Sezione:</strong>
  <%= Sezione.find(Alunno.find(@assenza.alunno_id).sezione_id).to_label %>
</p>

<p>
  <strong>Inserito da:</strong>
  <% if Amministrazione.exists?(@assenza.amministrazione_id) %>
  <%= Amministrazione.find(@assenza.amministrazione_id).cognome %>
  <%= Amministrazione.find(@assenza.amministrazione_id).nome %>
  <%else%>
  Utente non più in archivio
  <%end%>
</p>


<% if session[:role] == 'amministrazione' && userlogged_in? %>
<%= link_to 'Edit', edit_assenza_path(@assenza) %> |
<%end%>
<%= link_to 'Back', assenze_path %>

<script src="assets/js/jquery.min.js"></script>
    <script src="assets/bootstrap/js/bootstrap.min.js"></script>
</body>

</html>

