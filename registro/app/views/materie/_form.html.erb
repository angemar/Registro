<%= form_for(@materia) do |f| %>
  <% if @materia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@materia.errors.count, "error") %> La materia non può essere inserita:</h2>

      <ul>
      <% @materia.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit "Invio", :class => 'button', :style=>'background:#FFE0B2'%>
  </div>
<% end %>
