<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elenco Materie</title>
    <link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="assets/css/styles.css">
</head>


<p id="notice"><%= notice %></p>

<body style="background:#FFA726;">
    <div class="col-md-12" style="text-align:center;">
        <h1> Lista delle Materie</h1></div>
    <div class="col-md-12">
        <div class="table-responsive", align="center">
			<table class="table", style="border-collapse:collapse;">

<thead>
				    <tr>
					  <th  style="text-align:center;border:1px solid #ddd;padding:8px;">
						<span>Nome</span>
					  </th>
					  <th  style="text-align:center;border:1px solid #ddd;padding:8px;">
						<span>Mostra</span>
					  </th>
					  <% if session[:role] == 'amministrazione' && userlogged_in? %>
					  <th  style="text-align:center;border:1px solid #ddd;padding:8px;">
						<span>Modifica</span>
					  </th>
					  <th  style="text-align:center;border:1px solid #ddd;padding:8px;">
						<span>Elimina</span>
					  </th>
					  <%end%>

    </tr>
  </thead>

  <tbody>
    <% @materie.each do |materia| %>
      <tr>
        <td style="text-align:center;border:1px solid #ddd;padding:8px;"><%= materia.nome %></td>
        <td style="text-align:center;border:1px solid #ddd;padding:8px;"><%= link_to 'Show', materia, :style=>"color:black" %></td>
        <% if session[:role] == 'amministrazione' && userlogged_in? %>
        <td style="text-align:center;border:1px solid #ddd;padding:8px;"><%= link_to 'Edit', edit_materia_path(materia) , :style=>"color:black"%></td>
        <td style="text-align:center;border:1px solid #ddd;padding:8px;"><%= link_to 'Destroy', materia, method: :delete, :style=>"color:black", data: { confirm: 'Sei Sicuro?' } %></td>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if session[:role] == 'amministrazione' && userlogged_in? %>
<%= link_to 'Nuova Materia', new_materia_path %>
<% end %>

        </div>
    </div>
    <script src="assets/js/jquery.min.js"></script>
    <script src="assets/bootstrap/js/bootstrap.min.js"></script>
</body>
